import { TierReputation } from "@/components/serviceprovider/ServiceProviderCommandCenter";

export const metadata = {
  title: "Tier & Reputation | Mondial",
  description: "Track Service Provider tier and reputation.",
};

export default function ServiceProviderTierPage() {
  return <TierReputation />;
}
