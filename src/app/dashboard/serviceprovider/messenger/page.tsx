import { ServiceProviderMessenger } from "@/components/serviceprovider/ServiceProviderCommandCenter";

export const metadata = {
  title: "Messenger | Mondial",
  description: "Service Provider lead, proposal, workroom, and payment messenger.",
};

export default function ServiceProviderMessengerPage() {
  return <ServiceProviderMessenger />;
}
