import { WorkroomProjects } from "@/components/serviceprovider/ServiceProviderCommandCenter";

export const metadata = {
  title: "Project Workroom | Mondial",
  description: "Manage a Service Provider project workroom.",
};

export default function ServiceProviderWorkroomPage() {
  return <WorkroomProjects />;
}
